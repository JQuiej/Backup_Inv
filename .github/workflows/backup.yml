name: Backup Dual con Rclone

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  backup_and_upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      # 1. Instalar herramientas PostgreSQL 17
      - name: Instalar herramientas PostgreSQL 17
        run: |
          sudo apt-get install -y wget gnupg2 lsb-release
          sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17

      # 2. Instalar Rclone
      - name: Instalar Rclone
        run: |
          sudo -v ; curl https://rclone.org/install.sh | sudo bash

      # 3. Configuramos Rclone
      - name: Configurar Rclone
        env:
          RCLONE_CONF: ${{ secrets.RCLONE_CONF }}
        run: |
          mkdir -p ~/.config/rclone
          echo "$RCLONE_CONF" | base64 --decode > ~/.config/rclone/rclone.conf

      # 4. Crear Backups y Subir
      - name: Generar Backups y Subir
        env:
          DATABASE_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          DATE=$(date +%Y-%m-%d)
          
          # Nombres de archivos
          FILE_SUPABASE="backup-supabase-$DATE.sql"
          FILE_POSTGRES="backup-postgres-clean-$DATE.sql"
          
          echo "📦 Generando versión SUPABASE..."
          # Este se queda igual (para restaurar en otra nube Supabase si hace falta)
          /usr/lib/postgresql/17/bin/pg_dump "$DATABASE_URL" -f "$FILE_SUPABASE"

          echo "🧹 Generando versión POSTGRES (Compatible pgAdmin)..."
          # AGREGAMOS: --inserts
          # Esto genera 'INSERT INTO tabla VALUES (...)' que funciona SIEMPRE en pgAdmin
          /usr/lib/postgresql/17/bin/pg_dump "$DATABASE_URL" --no-owner --no-acl --inserts -f "$FILE_POSTGRES"

          echo "🚀 Subiendo a Google Drive..."
          rclone copy "$FILE_SUPABASE" midrive:Backups
          rclone copy "$FILE_POSTGRES" midrive:Backups
          
          echo "✅ ¡Éxito!"
